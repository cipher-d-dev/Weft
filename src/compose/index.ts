/**
 * @compose — Compose pipeline barrel export
 *
 * @example
 * import { compose } from '@compose';
 * const semantics = compose('glass', ['vision', 'motor']);
 */

export { compose } from './compose';
